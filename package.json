{
  "name": "heap_with_nodejs",
  "version": "1.0.0",
  "description": "heap with nodejs",
  "main": "index.js",
  "scripts": {
    "compile": "tsc src/*.ts --outDir dist",
    "clear": "rm dist/*.js",
    "start": "npm run build:live",
    "build:live": "nodemon --exec ./node_modules/.bin/ts-node -- ./src/index.ts"
  },
  "keywords": [
    "heap",
    "priority queue"
  ],
  "author": "json",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "jest": "^27.0.6",
    "nodemon": "^2.0.12",
    "ts-jest": "^27.0.3",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  }
}
